<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ritudey.departmentmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DepartmentManagement">
        <activity android:name=".Activities.StudentInfoActivity"></activity>
        <activity android:name=".Activities.QnaActivity" />
        <activity android:name=".Activities.NotesActivity" />
        <activity android:name=".Activities.NoticeActivity" />
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>